{
  "name": "knockr",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn workspace @knockr/mui build:iife",
    "build:storybook": "yarn workspace @knockr/mui build:storybook",
    "dev": "yarn workspace @knockr/mui dev",
    "preview": "yarn workspace @knockr/mui preview"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "prettier-plugin-organize-imports": "^2.3.4",
    "serve": "^13.0.2",
    "typescript": "^4.5.4"
  }
}
